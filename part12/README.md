# Part 12 – Containers (Docker)

This directory demonstrates containerising a full-stack Node/Express application with Docker and docker-compose.

## Running

```bash
docker-compose up
```

The app will be available at `http://localhost:3000` (frontend) and `http://localhost:3001` (API).
